# bk-task dependencies
# Shared with other skills via common venv

# Testing
pytest>=7.0.0

# Date parsing
python-dateutil>=2.8.0

# YAML for master.yaml
pyyaml>=6.0

# HTTP requests (already installed via common)
requests>=2.28.0
